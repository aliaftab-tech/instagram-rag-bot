/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed — Vercel handles everything.
  // If you need to allow external image domains later, add them here.
};

export default nextConfig;
